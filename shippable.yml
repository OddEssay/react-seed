language: node_js
node_js:
  - 6.2.0 
  - 4.2.3
env:
  - NODE_ENV=production
build:
  pre_ci_boot:
    image_name: drydock/u14nod
    image_tag: prod
    pull: true
  ci:
    - npm install
    - npm test
  cache: true
  cache_dir_list:
    - $SHIPPABLE_BUILD_DIR/node_modules
